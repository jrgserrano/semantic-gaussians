[project]
name = "scannetpp"
version = "0.1.0"
requires-python = "<3.13"
dependencies = [
    "munch>=4.0.0",
    "numpy>=2.0.0",
    "pyyaml>=6.0.2",
    "tqdm>=4.67.1",
    "torch",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
